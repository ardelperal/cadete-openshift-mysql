# Archivos sensibles (no commitear)
openshift_config.json
openshift_push_auth.json
*_auth.json
*.env

# Backups y artefactos
backups/

# Excluir SQL exportados por ops-sync
ops-sync/*.sql
ops-sync/*.sql.gz
